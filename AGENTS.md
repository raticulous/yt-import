# Command Execution Policy
- You are authorized to run read-only, build, test, lint, and routine git commands automatically.
- NEVER run destructive commands (such as recursive file deletion, hard git resets, or dropping database tables) without explicitly proposing the command and asking for user confirmation first.